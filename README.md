
# متجر "Fabrics" للستائر الفاخرة: برومبت إنشاء نسخة طبق الأصل

هذا المستند هو "برومبت" أو وصف تفصيلي لإنشاء نسخة طبق الأصل من متجر "Fabrics" الإلكتروني، وهو متجر حديث ومتكامل متخصص في بيع الستائر الفاخرة. تم بناء المشروع باستخدام **Next.js** و **Tailwind CSS** و **ShadCN UI**، مع دمج تقنيات الذكاء الاصطناعي باستخدام **Genkit**.

---

## 🚀 كيفية حفظ المشروع على جهازك (الشرح المبسط)

اتبع هذه الخطوات البسيطة، وإذا واجهت أي مشكلة، فقط أخبرني.

### الخطوة 1: إنشاء مستودع جديد على GitHub

1.  اذهب إلى [GitHub.com](https://github.com) وسجل الدخول إلى حسابك.
2.  اضغط على علامة `+` في الزاوية العلوية اليمنى، ثم اختر **"New repository"**.
3.  أعطِ المستودع أي اسم تريده (مثلاً: `my-curtain-store`).
4.  **مهم جدًا:** اترك المستودع **فارغًا تمامًا**. لا تقم بتحديد أي خيار مثل "Add a README file" أو "Add .gitignore".
5.  اضغط على الزر الأخضر **"Create repository"**.

### الخطوة 2: ربط المشروع ورفع الكود

بعد إنشاء المستودع، ستظهر لك صفحة على GitHub بها بعض الأوامر. ستحتاج إلى نسخ الرابط من هذه الصفحة.

1.  **انسخ رابط (URL) المستودع الخاص بك**. سيبدو بهذا الشكل:
    ```
    https://github.com/your-username/my-curtain-store.git
    ```

2.  **افتح الطرفية (Terminal)** في بيئة التطوير هذه.
    *   عادة ما تكون هناك أيقونة للطرفية أو يمكنك البحث عنها في القائمة.

3.  **نفذ الأوامر التالية بالترتيب** داخل الطرفية:

    أ. **لتهيئة المشروع (إذا كانت هذه المرة الأولى):**
    ```bash
    git init -b main
    ```

    ب. **لإضافة جميع الملفات:**
    ```bash
    git add .
    ```

    ج. **لحفظ التغييرات:**
    ```bash
    git commit -m "Initial project setup"
    ```

    د. **لربط مشروعك بالمستودع الجديد** (استبدل `[الرابط الذي نسخته]` بالرابط الفعلي لمستودعك):
    ```bash
    git remote add origin [الرابط الذي نسخته]
    ```

    هـ. **لرفع الكود إلى GitHub:**
    ```bash
    git push -u origin main
    ```

### الخطوة 3: تحميل المشروع على جهازك (Clone)

الآن بعد أن أصبح الكود على GitHub، يمكنك تحميله على جهازك بسهولة.

1.  **على جهاز الكمبيوتر الخاص بك**، افتح الطرفية (مثل Git Bash أو Command Prompt).
2.  اذهب إلى المجلد الذي تريد حفظ المشروع فيه (مثلاً `cd Documents/Projects`).
3.  نفذ أمر `git clone` مع رابط المستودع:
    ```bash
    git clone [الرابط الذي نسخته]
    ```
    سيقوم هذا الأمر بإنشاء مجلد جديد على جهازك بنفس اسم المستودع، وبداخله كل ملفات المشروع.

### ✨ الخطوة 4: كيفية فتح وتشغيل المشروع (مهم جدًا)

لقد قمت الآن بتحميل كل الملفات، ولكنك لم "تفتح المشروع" بعد. أنت ترى ملف `README` فقط لأنك فتحت ملفًا واحدًا. **يجب عليك فتح المجلد بأكمله** باستخدام محرر أكواد.

1.  **افتح محرر الأكواد (مثل VS Code):**
    *   شغّل برنامج محرر الأكواد الذي تستخدمه.

2.  **افتح المجلد الكامل للمشروع:**
    *   من داخل المحرر، اذهب إلى `File` > `Open Folder` (ملف > فتح مجلد).
    *   اختر المجلد الذي تم إنشاؤه في الخطوة السابقة (اسمه `my-curtain-store` أو الاسم الذي اخترته).
    *   الآن سترى جميع ملفات المشروع منظمة في القائمة الجانبية.

3.  **تثبيت الحزم وتشغيل المشروع:**
    *   من داخل محرر الأكواد، افتح الطرفية (Terminal) الخاصة به (عادة من قائمة `Terminal` > `New Terminal`).
    *   **اكتب الأمر التالي** لتثبيت جميع الحزم اللازمة:
        ```bash
        npm install
        ```
    *   **بعد انتهاء التثبيت**، اكتب الأمر التالي لتشغيل المشروع:
        ```bash
        npm run dev
        ```

4.  **شاهد مشروعك:**
    *   سيظهر لك رابط في الطرفية (عادة `http://localhost:9002`). افتحه في متصفح الإنترنت لترى موقعك يعمل!

إذا واجهت أي خطأ، فقط انسخ رسالة الخطأ وأرسلها لي.

---

## ⚙️ الإعداد الأولي (مهم)

### 1. مفتاح Gemini API
لاستخدام ميزات الذكاء الاصطناعي في المتجر (مثل مساعد التصميم والتجربة الافتراضية)، يجب عليك ربط حسابك الخاص في **Gemini**.

1.  **أنشئ ملفًا جديدًا** في المجلد الرئيسي لمشروعك وسمّه `.env` (إذا لم يكن موجودًا).
2.  **أضف السطر التالي** داخل ملف `.env` مع استبدال `YOUR_API_KEY` بمفتاح API الخاص بك الذي تحصل عليه من Google AI Studio:

    ```env
    GEMINI_API_KEY=YOUR_API_KEY
    ```

### 2. كلمة مرور لوحة التحكم
للوصول إلى لوحة تحكم إدارة الطلبات (`/admin`)، يجب عليك تعيين كلمة مرور خاصة للمدير. هذا الإعداد حاليًا غير مستخدم لأن لوحة التحكم عامة، ولكنه قد يكون مفيدًا في المستقبل.

1.  **افتح ملف `.env`**.
2.  **ابحث عن السطر التالي** وقم بتغيير `change-me-12345` إلى كلمة مرور قوية من اختيارك:
    ```env
    ADMIN_PASSWORD="change-me-12345"
    ```
