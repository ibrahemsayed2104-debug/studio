
# متجر "Fabrics" للستائر الفاخرة: برومبت إنشاء نسخة طبق الأصل

هذا المستند هو "برومبت" أو وصف تفصيلي لإنشاء نسخة طبق الأصل من متجر "Fabrics" الإلكتروني، وهو متجر حديث ومتكامل متخصص في بيع الستائر الفاخرة. تم بناء المشروع باستخدام **Next.js** و **Tailwind CSS** و **ShadCN UI**، مع دمج تقنيات الذكاء الاصطناعي باستخدام **Genkit**.

---

## 🚀 كيفية حفظ المشروع على جهازك (الشرح المبسط)

لحفظ نسخة كاملة من هذا المشروع على جهاز الكمبيوتر الخاص بك، أفضل طريقة هي استخدام GitHub. اتبع هذه الخطوات البسيطة، وإذا واجهت أي مشكلة، فقط أخبرني.

### الخطوة 1: إنشاء مستودع جديد على GitHub

1.  اذهب إلى [GitHub.com](https://github.com) وسجل الدخول إلى حسابك.
2.  اضغط على علامة `+` في الزاوية العلوية اليمنى، ثم اختر **"New repository"**.
3.  أعطِ المستودع أي اسم تريده (مثلاً: `my-curtain-store`).
4.  **مهم جدًا:** اترك المستودع **فارغًا تمامًا**. لا تقم بتحديد أي خيار مثل "Add a README file" أو "Add .gitignore".
5.  اضغط على الزر الأخضر **"Create repository"**.

### الخطوة 2: ربط المشروع ورفع الكود

بعد إنشاء المستودع، ستظهر لك صفحة على GitHub بها بعض الأوامر. ستحتاج إلى نسخ الرابط من هذه الصفحة.

1.  **انسخ رابط (URL) المستودع الخاص بك**. سيبدو بهذا الشكل:
    ```
    https://github.com/your-username/my-curtain-store.git
    ```

2.  **افتح الطرفية (Terminal)** في بيئة التطوير هذه.
    *   عادة ما تكون هناك أيقونة للطرفية أو يمكنك البحث عنها في القائمة.

3.  **نفذ الأوامر التالية بالترتيب** داخل الطرفية:

    أ. **لتهيئة المشروع (إذا كانت هذه المرة الأولى):**
    ```bash
    git init -b main
    ```

    ب. **لإضافة جميع الملفات:**
    ```bash
    git add .
    ```

    ج. **لحفظ التغييرات:**
    ```bash
    git commit -m "Initial project setup"
    ```

    د. **لربط مشروعك بالمستودع الجديد** (استبدل `[الرابط الذي نسخته]` بالرابط الفعلي لمستودعك):
    ```bash
    git remote add origin [الرابط الذي نسخته]
    ```

    هـ. **لرفع الكود إلى GitHub:**
    ```bash
    git push -u origin main
    ```

### الخطوة 3: تحميل المشروع على جهازك

الآن بعد أن أصبح الكود على GitHub، يمكنك تحميله على جهازك بسهولة.

1.  **على جهاز الكمبيوتر الخاص بك**، افتح الطرفية (مثل Git Bash أو Command Prompt).
2.  اذهب إلى المجلد الذي تريد حفظ المشروع فيه.
3.  نفذ أمر `git clone` مع رابط المستودع:
    ```bash
    git clone [الرابط الذي نسخته]
    ```

وهكذا، سيكون لديك نسخة كاملة من المشروع على جهازك! إذا واجهت أي خطأ، فقط انسخ رسالة الخطأ وأرسلها لي.

---

## ⚙️ الإعداد الأولي (مهم)

### 1. مفتاح Gemini API
لاستخدام ميزات الذكاء الاصطناعي في المتجر (مثل مساعد التصميم والتجربة الافتراضية)، يجب عليك ربط حسابك الخاص في **Gemini**.

1.  **أنشئ ملفًا جديدًا** في المجلد الرئيسي لمشروعك وسمّه `.env` (إذا لم يكن موجودًا).
2.  **أضف السطر التالي** داخل ملف `.env` مع استبدال `YOUR_API_KEY` بمفتاح API الخاص بك الذي تحصل عليه من Google AI Studio:

    ```env
    GEMINI_API_KEY=YOUR_API_KEY
    ```

### 2. كلمة مرور لوحة التحكم
للوصول إلى لوحة تحكم إدارة الطلبات (`/admin`)، يجب عليك تعيين كلمة مرور خاصة للمدير. هذا الإعداد حاليًا غير مستخدم لأن لوحة التحكم عامة، ولكنه قد يكون مفيدًا في المستقبل.

1.  **افتح ملف `.env`**.
2.  **ابحث عن السطر التالي** وقم بتغيير `change-me-12345` إلى كلمة مرور قوية من اختيارك:
    ```env
    ADMIN_PASSWORD="change-me-12345"
    ```

## ✨ الميزات الرئيسية

### 🛍️ تجربة التسوق الأساسية
- **كتالوج المنتجات:** صفحة رئيسية تعرض مجموعة مميزة من الستائر مع صور عالية الجودة وتصاميم جذابة.
- **صفحات تفاصيل المنتج:** صفحة مخصصة لكل منتج تتيح للعميل تخصيص الستارة بالكامل (اختيار نوع القماش، المقاس، اللون، والستايل).
- **سلة التسوق:** سلة تسوق تفاعلية تظهر في نافذة جانبية (`Sheet`)، تتيح للعملاء إضافة المنتجات، تعديل الكميات، أو إزالتها بسهولة.
- **عملية طلب مبسطة (بدون تسجيل دخول):**
    - لا حاجة لإنشاء حساب أو تسجيل الدخول.
    - يتم جمع بيانات العميل (الاسم، الهاتف، العنوان التفصيلي مع دعم خاص لمحافظات مصر ومدن السعودية).
    - الدفع عند الاستلام هو طريقة الدفع المعتمدة (مع ملاحظة دفع جزء مقدم).
    - يتم تأكيد الطلب عبر **إعادة توجيه العميل إلى واتساب** مع رسالة جاهزة تحتوي على جميع تفاصيل الطلب، ليقوم بإرسالها مباشرة إلى رقم المتجر.

### 🔒 لوحة تحكم للمدير
- **وصول مباشر:** لوحة التحكم (`/admin/dashboard`) متاحة للجميع حاليًا لسهولة الإدارة.
- **إدارة الطلبات:** تتيح للمدير عرض جميع الطلبات الواردة، الاطلاع على تفاصيل كل طلب (معلومات العميل، المنتجات، التخصيص)، وتحديث حالة الطلب (قيد المعالجة، تم الشحن، تم التوصيل، إلخ).

### 🤖 أدوات الذكاء الاصطناعي المبتكرة (Genkit)
- **مساعد التصميم الذكي (`/design-assistant`):**
    - يمكن للعملاء وصف ديكور غرفتهم (الألوان، الأثاث، النمط العام).
    - يقوم الذكاء الاصطناعي (باستخدام Genkit و Gemini) بتحليل الوصف وتقديم توصيات احترافية باللغة العربية.
- **التجربة الافتراضية (`/virtual-mockup`):**
    - تتيح للعملاء رفع صورة لغرفتهم الخاصة.
    - يقوم الذكاء الاصطناعي بدمج صورة الستارة المختارة مع صورة الغرفة، ليتمكن العميل من رؤية كيف ستبدو الستارة في منزله.

### 🖼️ معرض الصور والفيديوهات
- **معرض أعمال احترافي (`/gallery`):**
    - يعرض صورًا وفيديوهات ملهمة لتصاميم ستائر متنوعة ومقسمة إلى فئات (كلاسيك، مودرن، فندقية، إلخ).
    - ميزة تكبير المحتوى (الصور والفيديو) لعرض أدق التفاصيل.

### 📞 التواصل وخدمة العملاء
- **صفحة تواصل معنا (`/contact`):** تحتوي على نموذج لإرسال الاستفسارات مباشرة عبر واتساب وعنوان المتجر على خرائط جوجل.
- **تتبع الطلبات (`/orders`):** صفحة "تتبع طلبك" تتيح للعميل إدخال رقم الطلب ورؤية حالته الحالية.

### 🚀 الأداء وتجربة المستخدم (UX)
- **سرعة فائقة:** تم تحسين أداء المتجر بشكل كبير عبر التحميل الكسول للصور، وتحسين تحميل الخطوط.
- **متجاوب مع جميع الأجهزة:** تم تصميم المتجر ليعمل بشكل مثالي على جميع الشاشات.
- **تحسينات واجهة المستخدم:** القائمة الجانبية على الهواتف تختفي تلقائيًا بعد اختيار أي رابط، مما يوفر تجربة تصفح بديهية وسريعة.
