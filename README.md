# متجر "Fabrics" للستائر الفاخرة: برومبت إنشاء نسخة طبق الأصل

هذا المستند هو "برومبت" أو وصف تفصيلي لإنشاء نسخة طبق الأصل من متجر "Fabrics" الإلكتروني، وهو متجر حديث ومتكامل متخصص في بيع الستائر الفاخرة. تم بناء المشروع باستخدام **Next.js** و **Tailwind CSS** و **ShadCN UI**، مع دمج تقنيات الذكاء الاصطناعي باستخدام **Genkit**.

## ⚙️ الإعداد الأولي (مهم)

### 1. مفتاح Gemini API
لاستخدام ميزات الذكاء الاصطناعي في المتجر (مثل مساعد التصميم والتجربة الافتراضية)، يجب عليك ربط حسابك الخاص في **Gemini**.

1.  **أنشئ ملفًا جديدًا** في المجلد الرئيسي لمشروعك وسمّه `.env` (إذا لم يكن موجودًا).
2.  **أضف السطر التالي** داخل ملف `.env` مع استبدال `YOUR_API_KEY` بمفتاح API الخاص بك الذي تحصل عليه من Google AI Studio:

    ```env
    GEMINI_API_KEY=YOUR_API_KEY
    ```

### 2. كلمة مرور لوحة التحكم
للوصول إلى لوحة تحكم إدارة الطلبات (`/admin`)، يجب عليك تعيين كلمة مرور خاصة للمدير.

1.  **افتح ملف `.env`**.
2.  **ابحث عن السطر التالي** وقم بتغيير `change-me-12345` إلى كلمة مرور قوية من اختيارك:
    ```env
    ADMIN_PASSWORD="change-me-12345"
    ```
3.  بعد تعيين كلمة المرور، يمكنك الوصول إلى لوحة التحكم عبر الذهاب إلى رابط `/admin/login`.

## ✨ الميزات الرئيسية

### 🛍️ تجربة التسوق الأساسية
- **كتالوج المنتجات:** صفحة رئيسية تعرض مجموعة مميزة من الستائر مع صور عالية الجودة وتصاميم جذابة.
- **صفحات تفاصيل المنتج:** صفحة مخصصة لكل منتج تتيح للعميل تخصيص الستارة بالكامل (اختيار نوع القماش، المقاس، اللون، والستايل).
- **سلة التسوق:** سلة تسوق تفاعلية تظهر في نافذة جانبية (`Sheet`)، تتيح للعملاء إضافة المنتجات، تعديل الكميات، أو إزالتها بسهولة.
- **عملية طلب مبسطة (بدون تسجيل دخول):**
    - لا حاجة لإنشاء حساب أو تسجيل الدخول.
    - يتم جمع بيانات العميل (الاسم، الهاتف، العنوان التفصيلي مع دعم خاص لمحافظات مصر ومدن السعودية).
    - الدفع عند الاستلام هو طريقة الدفع المعتمدة.
    - يتم تأكيد الطلب عبر **إعادة توجيه العميل إلى واتساب** مع رسالة جاهزة تحتوي على جميع تفاصيل الطلب، ليقوم بإرسالها مباشرة إلى رقم المتجر.

### 🔒 لوحة تحكم للمدير
- **تسجيل دخول آمن:** صفحة تسجيل دخول محمية (`/admin/login`) باستخدام كلمة مرور يتم تعيينها في ملف `.env`.
- **إدارة الطلبات:** لوحة تحكم (`/admin/dashboard`) تتيح للمدير عرض جميع الطلبات الواردة، الاطلاع على تفاصيل كل طلب (معلومات العميل، المنتجات، التخصيص)، وتحديث حالة الطلب (قيد المعالجة، تم الشحن، تم التوصيل، إلخ).

### 🤖 أدوات الذكاء الاصطناعي المبتكرة (Genkit)
- **مساعد التصميم الذكي (`/design-assistant`):**
    - يمكن للعملاء وصف ديكور غرفتهم (الألوان، الأثاث، النمط العام).
    - يقوم الذكاء الاصطناعي (باستخدام Genkit و Gemini) بتحليل الوصف وتقديم توصيات احترافية باللغة العربية.
- **التجربة الافتراضية (`/virtual-mockup`):**
    - تتيح للعملاء رفع صورة لغرفتهم الخاصة.
    - يقوم الذكاء الاصطناعي بدمج صورة الستارة المختارة مع صورة الغرفة، ليتمكن العميل من رؤية كيف ستبدو الستارة في منزله.

### 🖼️ معرض الصور والتصميم
- **معرض صور احترافي (`/gallery`):**
    - يعرض صورًا ملهمة لتصاميم ستائر متنوعة ومقسمة إلى فئات.
    - ميزة تكبير الصورة لعرض أدق التفاصيل.

### 📞 التواصل وخدمة العملاء
- **صفحة تواصل معنا (`/contact`):** تحتوي على نموذج لإرسال الاستفسارات مباشرة عبر واتساب وعنوان المتجر على خرائط جوجل.
- **تتبع الطلبات:** صفحة "تتبع طلبك" تتيح للعميل إدخال رقم الطلب ورؤية حالته الحالية.

### 🚀 الأداء وتجربة المستخدم (UX)
- **سرعة فائقة:** تم تحسين أداء المتجر بشكل كبير عبر التحميل الكسول للصور، وتحسين تحميل الخطوط.
- **متجاوب مع جميع الأجهزة:** تم تصميم المتجر ليعمل بشكل مثالي على جميع الشاشات.
- **تحسينات واجهة المستخدم:** القائمة الجانبية على الهواتف تختفي تلقائيًا بعد اختيار أي رابط، مما يوفر تجربة تصفح بديهية وسريعة.
